server:
  address: 0.0.0.0
  port: ${CATALOG_SERVER_PORT}

spring:
  application:
    name: catalog-service

  output:
    ansi:
      enabled: always

  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
